# Prefer deploy/setup.sh HOSTNAME: it generates real database/dashboard secrets.
DOMAIN=knowledge.example.com
POSTGRES_PASSWORD=replace-with-generated-hex-password
UI_PASSWORD_HASH='replace-with-caddy-bcrypt-hash'
HINDSIGHT_IMAGE=ghcr.io/vectorize-io/hindsight:0.10.0
POSTGRES_IMAGE=pgvector/pgvector:0.8.2-pg17
NGINX_IMAGE=nginx:1.30.5-alpine
CADDY_IMAGE=caddy:2-alpine
